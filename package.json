{
  "name": "persona-mirror",
  "private": true,
  "workspaces": [
    "backend",
    "frontend",
    "shared"
  ],
  "scripts": {
    "backend:dev": "npm run dev -w backend",
    "shared:build": "npm run build -w shared"
  }
}
