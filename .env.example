# === BACKEND ENVIRONMENT VARIABLES (Supabase Edge Functions / Deno) ===
SUPABASE_URL=https://xxxx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJ... (Admin level secret role key)
GEMINI_API_KEY=AIzaSy... (Google Gemini API Key for Primary LLM Engine)
GROQ_API_KEY=gsk_... (Groq API Key for Fallback Llama 3.3 Engine)

# === FRONTEND ENVIRONMENT VARIABLES (Flutter / Dart) ===
# Pass these via --dart-define or use a flutter_dotenv package setup
SUPABASE_ANON_KEY=eyJ... (Public anonymous database key)
BACKEND_BASE_URL=https://xxxx.supabase.co/functions/v1
